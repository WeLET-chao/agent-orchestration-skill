[project]
name = "repo-daily-summary"
version = "0.1.0"
description = "Advisor-ready repository progress summary tooling"
requires-python = ">=3.12,<3.13"
dependencies = [
    "python-pptx>=1.0.2,<2",
]

[tool.uv]
package = false
